
namespace HexadecimalInteger
{
    using System;

    class HexadecimalInteger
    {
        static void Main(string[] args)
        {
            /*
             * Declare an integer variable and assign it with
             * the value 254 in hexadecimal format.
             * Use Windows Calculator to find its hexadecimal representation.
            */
            int hexadecimalVariable = 0xFE;
            Console.WriteLine("Hexadecimal FE is equal to decimal {0}", hexadecimalVariable);
        }
    }
}
